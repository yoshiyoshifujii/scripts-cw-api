# How to use

```
> CHATWORK_TOKEN=xxx REMOVE_ACCOUNT_ID=012345 sbt run

Room Name: hoge
Before  RoomMembers: Vector(RoomMember(012,admin,<account name>), RoomMember(013,admin,<account name>)
After   RoomMembers: Vector(RoomMember(012,admin,<account name>))
Remove Room Member ok?yes
Removed RoomMembers: PutOk(Vector(012),Vector(),Vector())
```
